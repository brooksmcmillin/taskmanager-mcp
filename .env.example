# TaskManager OAuth Credentials
# Register an OAuth client in TaskManager with redirect URI: https://your-auth-server.com/oauth/callback
TASKMANAGER_CLIENT_ID=brooks
TASKMANAGER_CLIENT_SECRET=your_secret_here

# Internal service URLs (for Docker networking)
MCP_AUTH_SERVER=http://auth-server:9000
TASKMANAGER_OAUTH_HOST=http://taskmanager:4321

# Public URLs (used for OAuth redirects and metadata)
# Update these with your actual domain names
MCP_AUTH_SERVER_PUBLIC_URL=https://mcp-auth.brooksmcmillin.com
MCP_SERVER_URL=https://mcp.brooksmcmillin.com

# Database URL for persistent token storage
# Uses the same database as taskmanager
DATABASE_URL=postgresql://taskmanager:your_password@postgres_db:5432/taskmanager
